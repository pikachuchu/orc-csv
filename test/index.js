var orc_csv = require('../lib-cov');
var fs = require('fs');
var nock = require('nock');

describe('orc-csv', function () {
  it('should sync from a path name', function () {

  });

  it('should sync from a file stream', function () {

  });

  it('should start a web server containing uploaded docs', function () {

  });
});
